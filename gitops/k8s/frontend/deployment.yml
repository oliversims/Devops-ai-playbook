apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: boutique
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: 742934034265.dkr.ecr.us-east-1.amazonaws.com/frontend:f99c9fb707fd65ae78fe0eb626816b797f6a8e8c
          ports:
            - containerPort: 3000
          # API URL is baked in at image build time (see frontend/Dockerfile). Not read at runtime.
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: boutique
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP

