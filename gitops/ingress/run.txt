# Deploy gitops/ingress — run from repo root

# 1. Update gitops/ingress/ingress.env (ACM cert + hostnames)
terraform -chdir=projects/Infrastructure/02_vpc output -raw acm_certificate_arn

# 2. Apply
kubectl apply -k gitops/ingress

# 3. Verify
kubectl get ingress -A

# Delete
kubectl delete -k gitops/ingress


